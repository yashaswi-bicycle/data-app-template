{
  "checkout-test-explorer.json": {
    "panels": [
      "kpi_tiles",
      "lift_by_dimension",
      "volume_vs_lift",
      "heatmap",
      "extremes"
    ],
    "queries": {
      "entity_list": "SELECT test_id, test_name, test_desc, test_status, test_group, test_end, unique_bookers FROM m_checkout_demo WHERE timestamp >= :from AND timestamp < :to ORDER BY unique_bookers DESC LIMIT 1000",
      "experiment_meta": "SELECT variant_name, test_desc, test_status, test_start, test_end, data_as_of, test_group, unique_bookers FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to",
      "arm_totals": "SELECT variant_name, arm_participants, control_participants, total_participants, unique_bookers, order_count, order_value FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to",
      "segments": "SELECT variant_name, device, market, customer_tier, unique_bookers, order_count, order_value FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to ORDER BY unique_bookers DESC LIMIT 10000",
      "daily_trend": "SELECT date_trunc('day', timestamp) AS day, variant_name, unique_bookers, order_count, order_value, arm_participants, control_participants, total_participants FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to"
    }
  },
  "checkout-test-report.json": {
    "panels": [
      "kpi_tiles",
      "hypothesis",
      "insights",
      "lifters_draggers",
      "cumulative_trend",
      "overview"
    ],
    "queries": {
      "entity_list": "SELECT test_id, test_name, test_desc, test_status, test_group, test_end, unique_bookers FROM m_checkout_demo WHERE timestamp >= :from AND timestamp < :to ORDER BY unique_bookers DESC LIMIT 1000",
      "experiment_meta": "SELECT variant_name, test_desc, test_status, test_start, test_end, data_as_of, test_group, unique_bookers FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to",
      "arm_totals": "SELECT variant_name, arm_participants, control_participants, total_participants, unique_bookers, order_count, order_value FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to",
      "segments": "SELECT variant_name, device, market, customer_tier, unique_bookers, order_count, order_value FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to ORDER BY unique_bookers DESC LIMIT 10000",
      "daily_trend": "SELECT date_trunc('day', timestamp) AS day, variant_name, unique_bookers, order_count, order_value, arm_participants, control_participants, total_participants FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to"
    }
  },
  "checkout-test-scorecard.json": {
    "panels": [
      "verdict",
      "kpi_tiles",
      "lift_by_dimension",
      "cumulative_trend"
    ],
    "queries": {
      "entity_list": "SELECT test_id, test_name, test_desc, test_status, test_group, test_end, unique_bookers FROM m_checkout_demo WHERE timestamp >= :from AND timestamp < :to ORDER BY unique_bookers DESC LIMIT 1000",
      "experiment_meta": "SELECT variant_name, test_desc, test_status, test_end, data_as_of, test_group, unique_bookers FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to",
      "arm_totals": "SELECT variant_name, arm_participants, control_participants, total_participants, unique_bookers, order_count, order_value FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to",
      "segments": "SELECT variant_name, device, market, unique_bookers, order_count, order_value FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to ORDER BY unique_bookers DESC LIMIT 10000",
      "daily_trend": "SELECT date_trunc('day', timestamp) AS day, variant_name, unique_bookers, order_count, order_value, arm_participants, control_participants, total_participants FROM m_checkout_demo WHERE test_id = :entity AND timestamp >= :from AND timestamp < :to"
    }
  },
  "retail-orders-changes.json": {
    "panels": [
      "kpis",
      "trend",
      "changes"
    ],
    "queries": {
      "totals": "SELECT orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to AND channel IN (:channel_0, :channel_1, :channel_2)",
      "by_time": "SELECT date_trunc('day', event_time) AS period, orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to AND channel IN (:channel_0, :channel_1, :channel_2)",
      "by_dimension": "SELECT region, channel, category, orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to ORDER BY orders_total DESC LIMIT 10000"
    }
  },
  "retail-orders-health-filtered.json": {
    "panels": [
      "kpis",
      "narrative",
      "trend",
      "breakdown",
      "ranking",
      "table"
    ],
    "queries": {
      "totals": "SELECT orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to AND region IN (:region_0, :region_1, :region_2) AND channel = :channel_0",
      "by_time": "SELECT date_trunc('day', event_time) AS period, orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to AND region IN (:region_0, :region_1, :region_2) AND channel = :channel_0",
      "by_dimension": "SELECT region, channel, category, orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to ORDER BY orders_total DESC LIMIT 10000"
    }
  },
  "retail-orders-health.json": {
    "panels": [
      "summary",
      "kpis",
      "trend",
      "breakdown",
      "ranking",
      "table"
    ],
    "queries": {
      "totals": "SELECT orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to",
      "by_time": "SELECT date_trunc('day', event_time) AS period, orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to",
      "by_dimension": "SELECT region, channel, category, orders_total, revenue_total, refund_rate_pct, avg_order_value FROM m_retail_demo WHERE event_time >= :from AND event_time < :to ORDER BY orders_total DESC LIMIT 10000"
    }
  },
  "retail-refund-watch.json": {
    "panels": [
      "verdict",
      "kpis",
      "breakdown",
      "trend",
      "heatmap",
      "trend"
    ],
    "queries": {
      "totals": "SELECT refund_count, refund_rate_pct, orders_total FROM m_retail_demo WHERE event_time >= :from AND event_time < :to",
      "by_time": "SELECT date_trunc('day', event_time) AS period, refund_count, refund_rate_pct, orders_total FROM m_retail_demo WHERE event_time >= :from AND event_time < :to",
      "by_dimension": "SELECT region, channel, category, refund_count, refund_rate_pct, orders_total FROM m_retail_demo WHERE event_time >= :from AND event_time < :to ORDER BY refund_count DESC LIMIT 10000",
      "by_time_region": "SELECT date_trunc('day', event_time) AS period, region, refund_count, refund_rate_pct, orders_total FROM m_retail_demo WHERE event_time >= :from AND event_time < :to",
      "by_time_category": "SELECT date_trunc('day', event_time) AS period, category, refund_count, refund_rate_pct, orders_total FROM m_retail_demo WHERE event_time >= :from AND event_time < :to"
    }
  }
}
